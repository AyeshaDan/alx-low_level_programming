#include <stdio.h>
#include "main.h"
/**
* _abs - the function to compute the absolute value of an integer
* 
* Return: Value 0 returns success
*/
int _abs(int n)
{
	int _abs = abs(n);

	printf("%d\n", _abs);

	return (0);
}
