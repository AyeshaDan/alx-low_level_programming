#include <stdlib.h>
#include "main.h"
/**
* _abs - the function to compute the absolute value of an integer
* @n: The value for any given number
* Return: Value 0 returns success
*/
int _abs(int n)
{
	int _abs = abs(n);

	return (_abs);
}
